package com.example.drivinglicenseexamapp.data.constants

enum class Mode {
    QUIZ_MODE,
    STUDY_MODE,
    RESULT_MODE
}

object VehicleType {
    const val BIKE = "bike"
    const val CAR = "car"
}